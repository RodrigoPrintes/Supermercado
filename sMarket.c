/* Aqui sera o main do programa
	creat by myself.
*/
#include "stdio.h"
#include "stdlib.h"
#include "funcoes.h"
#include "vd_dinamico.h"
void main(){
	int num_pvds , mda;
	int *fa;
	/*	num_pvds = numero de posto de vendas
		fa = fator de agilidade
		mds = medida de agilidade do pvds 
	*/
	// criar_atendente(fa,mda)
	int novo_num_pvds ;
	int  *novo_fa;
	//tipo_acao(string_Tipo);
	//C -> cliente,S-> descanso,F->finish_all
	int X_tempo,Y_tempo,Z_tempo;// descricao no cliente
	char eventos[2];
	scanf("%d",&num_pvds);
	int i;
	fa = newVd(num_pvds);
	// for(i=0;i<num_pvds){
	// 	int n_fa;
	// 	scanf("%d",&n_fa);
		
	// 	inserirVd(fa,n)
	// }
//	inserirFa(num_pvds);
	//scanf("%d",&novo_num_pvds);
	//novo_fa = newVd(num_pvds);
//	inserirFa(num_pvds);
}